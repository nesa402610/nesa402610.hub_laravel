export interface IAnimeStudio {
    id: number;
    name: string;
}

export interface IAnimeVideos {
    team: string
    id?: number,
    link: string,
    player: string,
    episode: number,
    iframe: boolean
}
